import { FC } from 'react';

const Header: FC = () => (
  <div className="navbar bg-white h-24 flex justify-end drop-shadow-md">
    <a className="btn btn-ghost normal-case text-2xl">Add List</a>
    <a className="btn btn-ghost normal-case text-2xl">Contact Us</a>
  </div>
);

export default Header;
